/* Определяем переменные */

const gulp = require('gulp'), // Сообственно Gulp JS
const uglify = require('gulp-uglify'), // Минификация JS
const concat = require('gulp-concat'), // Склейка файлов
const imagemin = require('gulp-imagemin'), // Минификация изображений
const csso = require('gulp-csso'), // Минификация CSS
const sass = require('gulp-sass'); // Конверстация SASS (SCSS) в CSS
